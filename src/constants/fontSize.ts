import { PixelRatio } from "react-native";

export const fontSize = {
  extraLight: PixelRatio.roundToNearestPixel(10),
  light: PixelRatio.roundToNearestPixel(12),
  semiRegular: PixelRatio.roundToNearestPixel(13),
  regular: PixelRatio.roundToNearestPixel(14),
  extraRegular: PixelRatio.roundToNearestPixel(15),
  medium: PixelRatio.roundToNearestPixel(16),
  semiMediumLarge: PixelRatio.roundToNearestPixel(17),
  semiLarge: PixelRatio.roundToNearestPixel(18),
  Large: PixelRatio.roundToNearestPixel(20),
  extraLarge: PixelRatio.roundToNearestPixel(24),
  semiHuge: PixelRatio.roundToNearestPixel(25),
  huge: PixelRatio.roundToNearestPixel(30),
  extraHuge: PixelRatio.roundToNearestPixel(32),
  extraExtraHuge: PixelRatio.roundToNearestPixel(36),
  extraExtraExtraHuge: PixelRatio.roundToNearestPixel(40),
  extraExtraExtraExtraHuge: PixelRatio.roundToNearestPixel(48),
};
